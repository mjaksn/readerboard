{
  "components": {
    "schemas": {
      "AlertRequest": {
        "additionalProperties": false,
        "description": "A message that takes the whole sign over until it is released.",
        "properties": {
          "display_mode": {
            "default": "HOLD",
            "description": "how the sign presents the alert",
            "title": "Display Mode",
            "type": "string"
          },
          "message": {
            "description": "the alert text. The sign's priority file holds 125 bytes once markup has been rendered, and cannot be resized. It can call variables with <var:name>, as a message can. It cannot be empty: a write with no text still carries the formatting bytes around it, which the sign reads as a blank priority message and displays, so the sign would sit blank with the rotation suppressed behind it and an alert reported as active",
            "maxLength": 4096,
            "minLength": 1,
            "title": "Message",
            "type": "string"
          },
          "ttl_seconds": {
            "anyOf": [
              {
                "exclusiveMinimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "release the sign this many seconds from now. Omit and the alert holds the sign until something releases it explicitly",
            "title": "Ttl Seconds"
          }
        },
        "required": [
          "message"
        ],
        "title": "AlertRequest",
        "type": "object"
      },
      "AlertResponse": {
        "description": "The alert currently holding the sign.",
        "properties": {
          "display_mode": {
            "title": "Display Mode",
            "type": "string"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          },
          "message": {
            "title": "Message",
            "type": "string"
          },
          "started_at": {
            "format": "date-time",
            "title": "Started At",
            "type": "string"
          }
        },
        "required": [
          "message",
          "display_mode",
          "started_at",
          "expires_at"
        ],
        "title": "AlertResponse",
        "type": "object"
      },
      "ClockResponse": {
        "description": "The result of setting the sign's clock.",
        "properties": {
          "synced_at": {
            "description": "when the clock was set, in its configured zone. Not what the sign was told: the sign is deliberately set one minute ahead, because the protocol's Set Time carries no seconds and a sign told the current minute runs behind for the rest of it",
            "format": "date-time",
            "title": "Synced At",
            "type": "string"
          }
        },
        "required": [
          "synced_at"
        ],
        "title": "ClockResponse",
        "type": "object"
      },
      "ControlCommandRequest": {
        "additionalProperties": false,
        "description": "A command aimed at the sign itself rather than at a message.",
        "properties": {
          "command": {
            "description": "one of SET_DAY_OF_WEEK, SET_TIME, SET_TIME_FORMAT, SOFT_RESET, SOUND, SPEAKER",
            "title": "Command",
            "type": "string"
          },
          "parameter": {
            "default": "",
            "description": "the command's parameter, if it takes one",
            "title": "Parameter",
            "type": "string"
          }
        },
        "required": [
          "command"
        ],
        "title": "ControlCommandRequest",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "HealthResponse": {
        "description": "What the service knows about itself. Requires no API key.",
        "properties": {
          "alert_active": {
            "title": "Alert Active",
            "type": "boolean"
          },
          "clock_last_synced_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Clock Last Synced At"
          },
          "link": {
            "$ref": "#/components/schemas/LinkHealth"
          },
          "sign_in_sync": {
            "description": "false when the sign is behind the service's record, which is a removal or an expiry that could not reach the sign yet; a message write that cannot reach the sign is refused with a 503 rather than held",
            "title": "Sign In Sync",
            "type": "boolean"
          },
          "slots_total": {
            "title": "Slots Total",
            "type": "integer"
          },
          "slots_used": {
            "title": "Slots Used",
            "type": "integer"
          },
          "status": {
            "description": "'ok' when the sign is reachable, otherwise 'degraded'",
            "title": "Status",
            "type": "string"
          },
          "variables_total": {
            "description": "0 when variables are switched off",
            "title": "Variables Total",
            "type": "integer"
          },
          "variables_used": {
            "title": "Variables Used",
            "type": "integer"
          },
          "version": {
            "title": "Version",
            "type": "string"
          }
        },
        "required": [
          "status",
          "version",
          "link",
          "slots_used",
          "slots_total",
          "variables_used",
          "variables_total",
          "sign_in_sync",
          "alert_active",
          "clock_last_synced_at"
        ],
        "title": "HealthResponse",
        "type": "object"
      },
      "LinkHealth": {
        "description": "The state of the link to the sign.",
        "properties": {
          "connected": {
            "title": "Connected",
            "type": "boolean"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Error"
          },
          "last_write_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Write At"
          },
          "suppressed_writes": {
            "description": "writes skipped because the sign already held those exact bytes",
            "title": "Suppressed Writes",
            "type": "integer"
          },
          "url": {
            "description": "the configured pyserial URL",
            "title": "Url",
            "type": "string"
          },
          "writes": {
            "title": "Writes",
            "type": "integer"
          }
        },
        "required": [
          "url",
          "connected",
          "last_write_at",
          "last_error",
          "writes",
          "suppressed_writes"
        ],
        "title": "LinkHealth",
        "type": "object"
      },
      "MessageRequest": {
        "additionalProperties": false,
        "description": "A message registered into a slot.",
        "properties": {
          "delete_on_expiry": {
            "default": true,
            "description": "what happens when ttl_seconds passes. True gives the slot back. False keeps the message registered and takes it off the display, clearing the deadline with it, so it can be shown again without being sent afresh. Nothing without a ttl_seconds",
            "title": "Delete On Expiry",
            "type": "boolean"
          },
          "display_mode": {
            "default": "HOLD",
            "description": "how the sign presents the message",
            "title": "Display Mode",
            "type": "string"
          },
          "message": {
            "description": "the message, including markup tokens such as <red> and <degree>, and <var:name> to call a variable, which has to exist first. It cannot be empty: an empty message holds a slot open around nothing, and the sign cycles to a file with no text in it. Use DELETE to give the slot back",
            "maxLength": 4096,
            "minLength": 1,
            "title": "Message",
            "type": "string"
          },
          "order": {
            "default": 0,
            "description": "lower numbers play earlier in the rotation; ties break on the slot name",
            "title": "Order",
            "type": "integer"
          },
          "source": {
            "anyOf": [
              {
                "maxLength": 128,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "who registered this, recorded so the slot list is readable",
            "title": "Source"
          },
          "ttl_seconds": {
            "anyOf": [
              {
                "exclusiveMinimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "act on the message this many seconds from now, deleting or hiding it according to delete_on_expiry; omit to keep it showing until it is replaced",
            "title": "Ttl Seconds"
          }
        },
        "required": [
          "message"
        ],
        "title": "MessageRequest",
        "type": "object"
      },
      "SignInformationResponse": {
        "description": "What the sign says about itself.",
        "properties": {
          "clock": {
            "description": "the sign's own clock, as HH:MM on a 24 hour dial",
            "title": "Clock",
            "type": "string"
          },
          "firmware_released": {
            "description": "the month and year of that firmware, as MM/YY",
            "title": "Firmware Released",
            "type": "string"
          },
          "firmware_revision": {
            "description": "its revision letter, empty on a sign that does not report one",
            "title": "Firmware Revision",
            "type": "string"
          },
          "firmware_version": {
            "description": "the firmware build the sign is running",
            "title": "Firmware Version",
            "type": "string"
          },
          "memory_free": {
            "description": "how much of that pool is unused, in bytes",
            "title": "Memory Free",
            "type": "integer"
          },
          "memory_total": {
            "description": "the size of the sign's memory pool, in bytes",
            "title": "Memory Total",
            "type": "integer"
          },
          "raw": {
            "description": "the sign's answer as it arrived, for when the fields are not enough",
            "title": "Raw",
            "type": "string"
          },
          "speaker_enabled": {
            "description": "whether the speaker will make a noise when SOUND is sent",
            "title": "Speaker Enabled",
            "type": "boolean"
          },
          "time_format": {
            "description": "how the sign draws <time>, '12 hour' or '24 hour'",
            "title": "Time Format",
            "type": "string"
          }
        },
        "required": [
          "firmware_version",
          "firmware_revision",
          "firmware_released",
          "clock",
          "time_format",
          "speaker_enabled",
          "memory_total",
          "memory_free",
          "raw"
        ],
        "title": "SignInformationResponse",
        "type": "object"
      },
      "SlotActiveRequest": {
        "additionalProperties": false,
        "description": "Whether the sign should be playing a slot.",
        "properties": {
          "active": {
            "description": "true to put the message into the rotation, false to take it off the display while it stays registered, keeping its slot, its file, its text and its place in the order",
            "title": "Active",
            "type": "boolean"
          }
        },
        "required": [
          "active"
        ],
        "title": "SlotActiveRequest",
        "type": "object"
      },
      "SlotResponse": {
        "description": "A registered slot.",
        "properties": {
          "active": {
            "description": "whether the sign is playing it; a false one stays registered and hidden",
            "title": "Active",
            "type": "boolean"
          },
          "delete_on_expiry": {
            "description": "whether ttl_seconds gives the slot back, or only hides the message",
            "title": "Delete On Expiry",
            "type": "boolean"
          },
          "display_mode": {
            "title": "Display Mode",
            "type": "string"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          },
          "key": {
            "title": "Key",
            "type": "string"
          },
          "label": {
            "description": "the sign file this slot occupies, A through Z",
            "title": "Label",
            "type": "string"
          },
          "message": {
            "title": "Message",
            "type": "string"
          },
          "order": {
            "title": "Order",
            "type": "integer"
          },
          "source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Source"
          },
          "updated_at": {
            "format": "date-time",
            "title": "Updated At",
            "type": "string"
          }
        },
        "required": [
          "key",
          "label",
          "message",
          "display_mode",
          "order",
          "active",
          "delete_on_expiry",
          "source",
          "expires_at",
          "updated_at"
        ],
        "title": "SlotResponse",
        "type": "object"
      },
      "TokenInfo": {
        "description": "One entry in an enumeration.",
        "properties": {
          "description": {
            "title": "Description",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name",
          "description"
        ],
        "title": "TokenInfo",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "ctx": {
            "title": "Context",
            "type": "object"
          },
          "input": {
            "title": "Input"
          },
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      },
      "VariableRequest": {
        "additionalProperties": false,
        "description": "A value for a variable, which the messages calling it show.",
        "properties": {
          "source": {
            "anyOf": [
              {
                "maxLength": 128,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "who wrote this, recorded so the variable list is readable",
            "title": "Source"
          },
          "stale_value": {
            "default": "",
            "description": "what to show once ttl_seconds has passed, such as --. Empty shows nothing. Checked against the variable's size now, not when it is needed",
            "maxLength": 1024,
            "title": "Stale Value",
            "type": "string"
          },
          "ttl_seconds": {
            "anyOf": [
              {
                "exclusiveMinimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "show stale_value in its place this many seconds from now unless a fresh value arrives first. The variable itself stays, since messages call it. Omit to keep the value until replaced",
            "title": "Ttl Seconds"
          },
          "value": {
            "description": "the value, with the same markup a message takes except <week_day> and <var:name>, which the sign draws as a literal character from inside a variable. Formatting set here carries on into the message after the call: a value of <red>DOWN turns the rest of the message red too. May be empty, which shows nothing where the variable is called",
            "maxLength": 1024,
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "value"
        ],
        "title": "VariableRequest",
        "type": "object"
      },
      "VariableResponse": {
        "description": "A variable.",
        "properties": {
          "called_by": {
            "description": "the keys of the slots whose messages call this variable. It cannot be deleted while any do, nor while called_by_alert is true",
            "items": {
              "type": "string"
            },
            "title": "Called By",
            "type": "array"
          },
          "called_by_alert": {
            "description": "true while the alert holding the sign calls this variable. It cannot be deleted until the alert is released or replaced with one that does not",
            "title": "Called By Alert",
            "type": "boolean"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "when the value goes stale, or null if it never will or already has, which stale tells apart",
            "title": "Expires At"
          },
          "label": {
            "description": "the sign file this variable occupies, a through z",
            "title": "Label",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Source"
          },
          "stale": {
            "description": "true once ttl_seconds has passed, when the sign shows stale_value instead",
            "title": "Stale",
            "type": "boolean"
          },
          "stale_value": {
            "title": "Stale Value",
            "type": "string"
          },
          "updated_at": {
            "format": "date-time",
            "title": "Updated At",
            "type": "string"
          },
          "value": {
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "name",
          "label",
          "value",
          "stale_value",
          "stale",
          "source",
          "expires_at",
          "updated_at",
          "called_by",
          "called_by_alert"
        ],
        "title": "VariableResponse",
        "type": "object"
      }
    },
    "securitySchemes": {
      "ApiKeyAuth": {
        "description": "The shared key every write carries. Set it with `api_key` in the config file or `READERBOARD_API_KEY` in the environment; `scripts/install.sh` generates one.",
        "in": "header",
        "name": "X-API-Key",
        "type": "apiKey"
      }
    }
  },
  "info": {
    "description": "\nDrives a BetaBrite Classic sign over the Alpha protocol, either through a serial\ncable or through an Ethernet to RS-232 adapter.\n\nSeveral sources can share the sign at once. Each registers a named **slot**, and\nthe sign rotates through the slots that are showing by itself. A slot can be\nhidden without being given up, so a message can be taken off the display and put\nback without being sent again. An **alert** takes the whole display over until it\nis released, then the rotation resumes.\n\nA **variable** is a value a slot's message or an alert calls with `<var:name>`.\nChanging it rewrites only the variable, so the sign shows the new value without\nblanking or restarting what calls it.\n\nEvery write needs an `X-API-Key` header, and so does `GET /sign/information`,\nwhich reads the sign rather than the service: it puts a question on the wire and\nholds the sign until the answer comes back. The service's own reads and\n`GET /health` need none. On this page, put the key in once with the\n**Authorize** button and everything below that needs it carries it.\n\nA failure is reported by the status code, with the reason in a `detail` field:\n400 for a command the sign does not have, a parameter it will not accept, a\nmessage or value too long for its file, markup the sign cannot render or a call\nto a variable that does not exist, 401 for a missing or wrong `X-API-Key`, 404\nfor a slot or variable that does not exist, 409 when every slot or every\nvariable is already in use or a variable something still calls is deleted, 503\nwhen the sign is unreachable, stops partway through an answer or answers with\nsomething the service cannot read, or no API key is configured at all, 500 for\nsomething the service has no code for, and 422 for a body that is not the shape\nthe endpoint declares, which includes a display mode the sign does not have.\n",
    "title": "readerboard",
    "version": "0.5.1"
  },
  "openapi": "3.1.0",
  "paths": {
    "/alerts": {
      "delete": {
        "description": "Release an alert so the rotation resumes.",
        "operationId": "delete_alert_alerts_delete",
        "responses": {
          "204": {
            "description": "Successful Response"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Give the sign back",
        "tags": [
          "Alerts"
        ]
      },
      "get": {
        "description": "Return the active alert, or null if the sign is rotating normally.",
        "operationId": "get_alert_alerts_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/AlertResponse"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "title": "Response Get Alert Alerts Get"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Read the alert holding the sign",
        "tags": [
          "Alerts"
        ]
      },
      "post": {
        "description": "Take the whole sign over until the alert is released.\n\nThis uses the sign's priority file, which suppresses every other message. If\na ttl is given, the sign is released automatically and the rotation resumes\nby itself.\n\nAn alert can call variables with `<var:name>`, and a change to one shows on\nthe alert without restarting it, as it does in a message. A variable the\nalert calls cannot be deleted until the alert is released or replaced, and a\ncall to a variable that does not exist is a 400.",
        "operationId": "post_alert_alerts_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AlertRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AlertResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Take the sign over with an alert",
        "tags": [
          "Alerts"
        ]
      }
    },
    "/enumerations/control-commands": {
      "get": {
        "description": "List every control command.",
        "operationId": "control_commands_enumerations_control_commands_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TokenInfo"
                  },
                  "title": "Response Control Commands Enumerations Control Commands Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Commands aimed at the sign itself",
        "tags": [
          "Enumerations"
        ]
      }
    },
    "/enumerations/display-modes": {
      "get": {
        "description": "List every display mode.",
        "operationId": "display_modes_enumerations_display_modes_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TokenInfo"
                  },
                  "title": "Response Display Modes Enumerations Display Modes Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Ways the sign can present a message",
        "tags": [
          "Enumerations"
        ]
      }
    },
    "/enumerations/markup-tokens": {
      "get": {
        "description": "List every token that can be written inline in a message, and the variable call.",
        "operationId": "markup_tokens_enumerations_markup_tokens_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TokenInfo"
                  },
                  "title": "Response Markup Tokens Enumerations Markup Tokens Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Markup tokens a message may contain",
        "tags": [
          "Enumerations"
        ]
      }
    },
    "/enumerations/value-tokens": {
      "get": {
        "description": "List the tokens a variable's value may contain.\n\nThe message tokens, less `<week_day>`, which the sign draws as a literal\ncharacter from inside a variable. A value cannot call another variable\neither, for the same reason.",
        "operationId": "value_tokens_enumerations_value_tokens_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TokenInfo"
                  },
                  "title": "Response Value Tokens Enumerations Value Tokens Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Markup tokens a variable's value may contain",
        "tags": [
          "Enumerations"
        ]
      }
    },
    "/health": {
      "get": {
        "description": "Report the state of the link, the slots, and the clock.\n\nDeliberately unauthenticated, so that a monitor can watch the sign\nwithout holding a key that could write to it.",
        "operationId": "health_health_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Is the service talking to the sign",
        "tags": [
          "Health"
        ]
      }
    },
    "/messages": {
      "delete": {
        "description": "Remove every slot, leaving the sign showing nothing.",
        "operationId": "clear_messages_messages_delete",
        "responses": {
          "204": {
            "description": "Successful Response"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Take every message off the sign",
        "tags": [
          "Messages"
        ]
      },
      "get": {
        "description": "Return every registered slot, in rotation order, hidden ones included.\n\nA hidden slot is listed like any other, with `active` false. It is still\nregistered and still holding its file; it is simply not one the sign plays.",
        "operationId": "list_messages_messages_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/SlotResponse"
                  },
                  "title": "Response List Messages Messages Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "List the messages sharing the sign",
        "tags": [
          "Messages"
        ]
      }
    },
    "/messages/{key}": {
      "delete": {
        "description": "Remove one slot and free the sign file it held.",
        "operationId": "delete_message_messages__key__delete",
        "parameters": [
          {
            "in": "path",
            "name": "key",
            "required": true,
            "schema": {
              "description": "the name of the slot, chosen by whoever owns it",
              "maxLength": 64,
              "minLength": 1,
              "pattern": "^[A-Za-z0-9._-]+$",
              "title": "Key",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Take a message off the sign",
        "tags": [
          "Messages"
        ]
      },
      "get": {
        "description": "Return one slot by name.",
        "operationId": "get_message_messages__key__get",
        "parameters": [
          {
            "in": "path",
            "name": "key",
            "required": true,
            "schema": {
              "description": "the name of the slot, chosen by whoever owns it",
              "maxLength": 64,
              "minLength": 1,
              "pattern": "^[A-Za-z0-9._-]+$",
              "title": "Key",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SlotResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read one message",
        "tags": [
          "Messages"
        ]
      },
      "put": {
        "description": "Put a message in a slot, replacing whatever was there.\n\nThe sign rotates through the slots that are showing on its own, so\nregistering a second message does not displace the first.\n\nThis leaves a hidden slot hidden. Whether a message is showing is not part of\nthe message, and a source re-sending the same content every few minutes would\notherwise switch a hidden one back on every time; `PUT /messages/{key}/active`\nis what moves that.",
        "operationId": "put_message_messages__key__put",
        "parameters": [
          {
            "in": "path",
            "name": "key",
            "required": true,
            "schema": {
              "description": "the name of the slot, chosen by whoever owns it",
              "maxLength": 64,
              "minLength": 1,
              "pattern": "^[A-Za-z0-9._-]+$",
              "title": "Key",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MessageRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SlotResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Register or replace a message",
        "tags": [
          "Messages"
        ]
      }
    },
    "/messages/{key}/active": {
      "put": {
        "description": "Take a message off the display, or put it back, keeping its slot either way.\n\nA hidden message stays registered. It keeps its slot, its file, its text and\nits place in the order, and is simply left out of the rotation the sign\ncycles, so showing it again needs no copy of what it said. This is separate\nfrom the message itself deliberately: a source that re-sends the same content\non a timer would otherwise switch a hidden message back on every time.\n\nHiding one disturbs nothing else on the sign; the other messages carry on\nwithout a blank or a restart. The hidden message's own file is emptied, which\nis what stops the sign freezing on it when it was the last one showing.\n\n404 when no slot by that name is registered.",
        "operationId": "set_message_active_messages__key__active_put",
        "parameters": [
          {
            "in": "path",
            "name": "key",
            "required": true,
            "schema": {
              "description": "the name of the slot, chosen by whoever owns it",
              "maxLength": 64,
              "minLength": 1,
              "pattern": "^[A-Za-z0-9._-]+$",
              "title": "Key",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SlotActiveRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SlotResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Show or hide a message without unregistering it",
        "tags": [
          "Messages"
        ]
      }
    },
    "/sign/command": {
      "post": {
        "description": "Send one of the sign's own control commands.\n\n`SOFT_RESET` restarts the sign. It erases nothing, the sign comes back\nshowing what it was showing, and it is the first thing to try on a sign that\nhas stopped responding. The display is blank for a few seconds while it runs\nits power-up diagnostics, and this call waits that out before answering, so\na 204 means the sign is listening again. Reach for `POST /sign/reboot` only\nwhen a soft reset is not enough: that one erases the sign and rebuilds it.",
        "operationId": "send_command_sign_command_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ControlCommandRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Send a control command to the sign",
        "tags": [
          "Sign"
        ]
      }
    },
    "/sign/information": {
      "get": {
        "description": "Read the sign's own account of itself.\n\nThe firmware build and the month it was released, the sign's clock and\nwhether it draws a 12 or 24 hour one, whether its speaker is enabled, and\nhow much of its memory pool is free. The protocol document calls this \"most\nuseful as a source of troubleshooting information\", which is a fair summary:\nnothing here changes anything.\n\nThis is the only read in the service, so it is also the only place a silent\nsign is distinguishable from an unplugged one. A sign that does not answer\nwithin a few seconds is a 503, the same as a sign that cannot be written to,\nand so is a sign that starts answering and does not finish, or answers with\nsomething this cannot read: the reply is the whole of what the endpoint has,\nso one that is cut short or will not parse leaves it with nothing to report.\n\nTwo fields are worth reading carefully. `speaker_enabled` is why `SOUND` can\nappear to do nothing: the protocol calls disabled the default, and a muted\nsign beeps silently. And `memory_free` is the pool the memory configuration\ndraws on, so a slot capacity that will not fit shows up here before it shows\nup as a failed reallocation.",
        "operationId": "sign_information_sign_information_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SignInformationResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Ask the sign what it is and how it is doing",
        "tags": [
          "Sign"
        ]
      }
    },
    "/sign/reboot": {
      "post": {
        "description": "Reset the sign to recover it, then put everything back.\n\n**A recovery tool, not a routine operation, and it is disruptive.** It sends\nthe memory clear that resets the sign, which erases every file on it and\ntakes the display blank for several seconds while the sign restarts. The\nservice then waits for the reset to finish and re-pushes every message and\nthe run sequence from its own record, so the sign comes back showing what it\nwas showing before rather than empty. Any active alert is re-asserted as\npart of that restore.\n\nUse it for a sign that has stopped responding to writes or is showing\ngarbage, the wedged-decoder state a unit mounted out of reach can fall into\nfrom a stray bit and that cannot be fixed by power cycling it by hand. Do\nnot use it to clear the sign: `DELETE /messages` takes every message off\nwithout resetting anything, and this puts them all straight back. Expect a\nblank display for roughly ten seconds before the rotation returns.\n\n503 if the sign cannot be reached, since a sign that is not answering cannot\nbe rebooted.",
        "operationId": "reboot_sign_sign_reboot_post",
        "responses": {
          "204": {
            "description": "Successful Response"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Reboot the sign to recover it, then restore the display",
        "tags": [
          "Sign"
        ]
      }
    },
    "/sign/sync-clock": {
      "post": {
        "description": "Set the sign's clock and day of week immediately.\n\nThe service already does this at startup, hourly, and whenever the link to\nthe sign comes back. This is for when you would rather not wait.\n\nThe sign is set one minute ahead of the real time, on purpose. Set Time\ncarries four digits and no seconds, so a sign told the current minute starts\nthat minute over and then reads behind for the rest of it, by up to\nfifty-nine seconds, and never ahead. Leading by a minute moves that\none-sided error to the side where the sign is at worst a minute fast and\nnever slow.",
        "operationId": "sync_clock_sign_sync_clock_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClockResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Set the sign's clock now",
        "tags": [
          "Sign"
        ]
      }
    },
    "/variables": {
      "get": {
        "description": "Return every variable, by name, with what calls each one.",
        "operationId": "list_variables_variables_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/VariableResponse"
                  },
                  "title": "Response List Variables Variables Get",
                  "type": "array"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "List the variables messages can call",
        "tags": [
          "Variables"
        ]
      }
    },
    "/variables/{name}": {
      "delete": {
        "description": "Delete a variable and free the sign file it held.\n\n409 while any message or the alert still calls it, naming what does. Its\nfile is written into each of those messages, so deleting it would leave them\ncalling a file the next variable could be given, and showing that variable's\nvalue.",
        "operationId": "delete_variable_variables__name__delete",
        "parameters": [
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "the variable's name, one to 32 lowercase letters, digits and underscores. A message calls it as <var:name>",
              "pattern": "^[a-z0-9_]{1,32}$",
              "title": "Name",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Delete a variable",
        "tags": [
          "Variables"
        ]
      },
      "get": {
        "description": "Return one variable by name.",
        "operationId": "get_variable_variables__name__get",
        "parameters": [
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "the variable's name, one to 32 lowercase letters, digits and underscores. A message calls it as <var:name>",
              "pattern": "^[a-z0-9_]{1,32}$",
              "title": "Name",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VariableResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Read one variable",
        "tags": [
          "Variables"
        ]
      },
      "put": {
        "description": "Set a variable's value, creating the variable if it does not exist yet.\n\nA message calls a variable with `<var:name>`, and every message calling it\nshows the new value the next time the sign draws it. Only the variable's own\nsmall file is written, so unlike a change to a message this does not blank\nthe display or restart the message: a scrolling message picks the new value\nup on its next pass. That makes a variable the way to show something that\nchanges often, such as a temperature or a count.\n\nFormatting in the value carries on after the call, so a value of `<red>DOWN`\nturns the rest of the message red as well. To keep a changing number from\nshifting the text around it, put `<fixed_width>` in the message before the\ncall and send values of the same length.\n\n409 when every variable is in use, and 400 when variables are switched off or\nthe value does not fit. A value that does not fit is refused rather than cut\nshort, because the sign does not cut it short either: it empties it.",
        "operationId": "put_variable_variables__name__put",
        "parameters": [
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "the variable's name, one to 32 lowercase letters, digits and underscores. A message calls it as <var:name>",
              "pattern": "^[a-z0-9_]{1,32}$",
              "title": "Name",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VariableRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VariableResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "ApiKeyAuth": []
          }
        ],
        "summary": "Create or change a variable",
        "tags": [
          "Variables"
        ]
      }
    }
  }
}
