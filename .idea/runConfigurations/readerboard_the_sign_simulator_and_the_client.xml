<component name="ProjectRunConfigurationManager">
  <!--
    All three at once: the simulator standing in for the sign, the service
    driving it, and the client calling the service. It is the same
    scripts/run_with_simulator.py as "readerboard and the sign simulator" with
    --with-client added, so there is still one place that decides what any of
    them is given. Run that script with its help flag to see the rest.

    Two things about it are worth knowing here. The client asks for the API key
    itself rather than taking one from a command line, so the key in use is
    printed in this window for pasting. And starting the client this way writes
    the base URL into the settings it remembers between runs, replacing an
    address you may have left in it.

    Closing the client leaves the other two running. Closing either of those
    stops the rest, because the service and the simulator are no use without
    each other.
  -->
  <configuration default="false" name="readerboard, the sign simulator and the client" type="PythonConfigurationType" factoryName="Python">
    <module name="readerboard" />
    <option name="ENV_FILES" value="" />
    <option name="INTERPRETER_OPTIONS" value="" />
    <option name="PARENT_ENVS" value="true" />
    <envs>
      <env name="PYTHONUNBUFFERED" value="1" />
    </envs>
    <option name="SDK_HOME" value="" />
    <option name="WORKING_DIRECTORY" value="$PROJECT_DIR$" />
    <option name="IS_MODULE_SDK" value="true" />
    <option name="ADD_CONTENT_ROOTS" value="true" />
    <option name="ADD_SOURCE_ROOTS" value="true" />
    <option name="SCRIPT_NAME" value="$PROJECT_DIR$/scripts/run_with_simulator.py" />
    <option name="PARAMETERS" value="--with-client" />
    <option name="SHOW_COMMAND_LINE" value="false" />
    <option name="EMULATE_TERMINAL" value="true" />
    <option name="MODULE_MODE" value="false" />
    <option name="REDIRECT_INPUT" value="false" />
    <option name="INPUT_FILE" value="" />
    <method v="2" />
  </configuration>
</component>
